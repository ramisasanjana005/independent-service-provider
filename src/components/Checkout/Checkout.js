import React from 'react';

const Checkout = () => {
    return (
        <div>
            <h2>Checkout</h2>
            <button className="btn btn-primary">Pay Now</button>
        </div>
    );
};

export default Checkout;